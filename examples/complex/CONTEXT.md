# Context

## Known Facts
- The project root contains `package.json`.

## Environment
- A shell and Node.js package manager are available.

## Assumptions
- The test entry, if present, is stored in `package.json` under `scripts.test`; absence stops with `E06-DEPENDENCY-MISSING`.

## Boundaries
- Do not edit project files or install new dependencies.
